---
---
- if item[:kind] == 'article'
  .meta
    %ul.tags
      %li= pretty_time(item[:created_at])
      - if item[:author]
        %li
          &middot;
        %li
          = "Posted by " + (item[:author])
      - if disqus_shortname 
        %li
          &middot;
        %li
          %a(href="#{route_path(item)}#disqus_thread")
      - if item[:tags]
        %li &middot;
        - item[:tags].sort.each do |tag|
          %li
            %a(href="/tags/#{tag.gsub(' ', '-').downcase}/" class='tag')
              = tag
